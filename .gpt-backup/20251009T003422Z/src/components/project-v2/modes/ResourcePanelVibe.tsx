'use client';
// Shim component to keep imports stable while consolidating UI to Ant Design
import React from "react";
export default function ResourcePanelVibe(props: any){
  return <div {...props}>Resource Panel (AntD variant placeholder)</div>;
}
